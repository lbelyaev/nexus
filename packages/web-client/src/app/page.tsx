import { NexusWebClient } from "../components/NexusWebClient";

export default function Page() {
  return (
    <main className="mx-auto h-full w-full max-w-[1600px] px-3 py-3 sm:px-5 sm:py-5">
      <NexusWebClient />
    </main>
  );
}
